arch:
  - arm64
  - amd64

addons:
  hosts:
    - node1

script:
  - df -hT
  - swapon test aarch64 and x86 in travis
  - sudo chmod 0600 /swapfile.img
  - sudo mkswap /swapfile.img
  - sudo swapon /swapfile.img
